export const HTTP_STATUS_CODES = Object.freeze({
  OK: 200,
  METHOD_NOT_ALLOWED: 405,
  BAD_REQUEST: 400,
});

export const HTTP_REQUEST_STATUS = Object.freeze({
  SUCCESS: "SUCCESS",
  FAILED: "FAILED",
});

export const HTTP_CONTENT_TYPES = Object.freeze({
  APPLICATION_JSON: "application/json",
});
